# Optional: set when frontend and backend run on different hosts (production).
# Leave unset in local dev — Vite proxies /api and /uploads to http://127.0.0.1:8000
# VITE_API_BASE_URL=http://127.0.0.1:8000
